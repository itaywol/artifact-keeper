apiVersion: v1
kind: Service
metadata:
  name: artifact-keeper
  namespace: artifact-keeper
spec:
  type: ClusterIP
  selector:
    app: artifact-keeper
  ports:
    - name: http
      port: 80
      targetPort: http
